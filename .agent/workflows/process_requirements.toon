---
description: Orchestrates the transformation of natural language requirements into engineering specifications using DDD and specialist agents.
---

# REQUIREMENT ORCHESTRATION WORKFLOW (TOON)

Este workflow define o pipeline de **Planejamento, Orquestração e Execução** para transformar desejos do usuário em sistemas funcionais.

## 👣 WORKFLOW STAGES

### 1. Planejamento (Planning Phase)
**Input**: Intenção do usuário e `PROJECT_INFO.toon`.

// turbo
1. **Iniciação**: Se `PROJECT_INFO.toon` estiver incompleto, o `ORCHESTRATOR` deve solicitar Nome, Objetivo e Stack.
// turbo
2. **Análise de Domínio**: Identificar Bounded Contexts e Ubiquitous Language.
// turbo
3. **Modelagem**: Gerar ou atualizar o `DDD_Model.toon`.

**Output**: `DDD_Model.toon`

### 2. Especificação (Engineering Phase)
**Input**: `DDD_Model.toon`.

// turbo
1. **Specs Técnicas**: Traduzir o modelo em `Engineering_Specs.toon`.
// turbo
2. **Contratos**: Definir endpoints, esquemas de banco (`Schema.toon`) e políticas de segurança (`Security_Policy.toon`).

**Output**: `Engineering_Specs.toon`

### 3. Orquestração (Delegation Phase)
O `ORCHESTRATOR` aciona os especialistas com base nas especificações:

- **SOFTWARE_ENGINEER**: Codificação e lógica de negócio.
- **DATABASE_MODELER**: Migrações e persistência.
- **SECURITY_AGENT**: JWT e IAM.
- **CAPACITY_AGENT**: Performance e escala.

### 4. Execução e Validação (Execution & Verification)
1. **Implementação**: Especialistas executam suas tarefas seguindo seus próprios workflows `👣`.
2. **Revisão de Coerência**: Validar se a implementação reflete o `DDD_Model.toon`.
3. **Sync**: Atualizar `PROJECT_STATUS.toon` e artefatos de conhecimento.

## 🛠️ TOOLKIT
- **Protocolo**: TOON (Token-Oriented Object Notation).
- **Modelos**: `DDD_Model.toon`, `Engineering_Specs.toon`.
- **Status**: `PROJECT_STATUS.toon`.
